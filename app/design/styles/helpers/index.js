import { _return, _return_container } from './_return';
import { _scrollbar } from './_scrollbar';
import { _menu_sub } from "./_menu_sub";

const helpers = {
    scrollbar: [_scrollbar],
    menu_sub:[_menu_sub],
    return: [_return, _return_container]
};

export default helpers;