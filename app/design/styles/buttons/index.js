import { _pill } from './_pill';
import { _pill_old } from './_pill_old';
import { _pill_section } from './_pill_section';

const buttons = [
	_pill_old, // [0]
	_pill, //[1]
	_pill_section // [2]
];

export default buttons;
